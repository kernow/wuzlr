<h1>Listing leagues</h1>

<table>
  <tr>
  </tr>

<% @leagues.each do |leagues| %>
  <tr>
    <td><%= link_to 'Show', leagues %></td>
    <td><%= link_to 'Edit', edit_leagues_path(leagues) %></td>
    <td><%= link_to 'Destroy', leagues, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New leagues', new_leagues_path %>